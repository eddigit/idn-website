import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Bulgarie et structuration européenne | IDN Global Advisory',
  description: 'EOOD, AD, structures opérationnelles en Bulgarie. État membre UE avec conditions compétitives pour configurations définies.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
